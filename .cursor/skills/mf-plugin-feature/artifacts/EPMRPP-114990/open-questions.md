# Open questions: EPMRPP-114990

| # | Question | Owner | Blocks | Status |
|---|----------|-------|--------|--------|
| 1 | What is the authoritative signal that "Mobitru video evidence exists" for a given test item? Attribute on the test item (`MBID`), attachments tagged with the `mobitru` log type, or a dedicated endpoint? Required for the plugin to decide its empty/null path. | BE / Plugin | nothing past Gate 1; impacts plugin implementation in Phase 4b | open |
| 2 | When no evidence exists, should the tab be **hidden** (no entry in the tab strip), **disabled** (visible but not clickable), or **empty** (clickable, content shows an empty state)? AC explicitly defers to Jira. The current contract supports the "empty" path natively; the other two require additional core support (host hiding empty extensions). | UX / PO | nothing past Gate 1; may add a follow-up to host `InfoTabs` if "hidden" is chosen | open |
| 3 | Concrete API used to list / stream Mobitru videos for a test item. Reuse the Attachments tab data flow vs. a direct Mobitru endpoint? Depends on US-LOG-MOB-002 finalisation. | BE / Plugin | nothing past Gate 1; impacts plugin implementation in Phase 4b | open |
| 4 | Multi-plugin support for the same slot — is one tab per extension acceptable, or should the host de-duplicate by label? Out of scope for the current story but would change the tab id policy. | UX / Architecture | nothing | open |
| 5 | Mobitru video container/codec and delivery scheme: plain MP4/WebM via signed URL, HLS manifest, or vendor-specific stream? Determines whether native `<video>` is sufficient or whether a player library (`react-player`/`plyr-react`) must be approved at Gate 1. | BE / Mobitru integration / Plugin | Phase 4b only — implementer must STOP and request a contract update if the format requires a NEW dep | open |
| 6 | Bundle-size budget for the plugin in case a video library becomes necessary. Is there a per-plugin budget enforced by the build or is the host's `bundle-stats` report advisory? Affects the choice between `react-player` and lighter alternatives. | UI lead / Architecture | nothing past Gate 1; only relevant if Q5 forces a NEW dep | open |
| 7 | ~~Tab label spelling — story / AC / i18n table say **"Remote devices" (plural)**, the Figma design says **"REMOTE DEVICE" (singular)**.~~ **Resolved 2026-05-08:** v1 uses the **singular** form ("Remote device") to match the design. Singular cascades through all naming (constant, selector, extension id, file paths, i18n key, tab id). Localisation Agent must request **singular** be/ru/uk translations from the story owner before Phase 5. | UX / PO | — | resolved |
| 8 | Mobitru session-metadata payload — exact field names for the eight rows in the design's `METADATA` panel (`ID`, `Device`, `Browser`, `OS`, `Automation Backend`, `Owner`, `Start Time`, `End Time`). The plugin needs the BE contract from US-LOG-MOB-002 to bind labels to values. | BE / Plugin | nothing past Gate 1; impacts plugin implementation in Phase 4b | open |
| 9 | ~~Multi-video UX — AC says "all videos available", design depicts a single player.~~ **Resolved 2026-05-08:** v1 scope is **single video per test item**; multi-video UX is a follow-up story. See `decision-sheet.md` Fork 7. | UX / PO | — | resolved |
| 10 | Token mapping pass — the Figma file does not declare design variables for this node (`get_variable_defs` is empty), so all colours / spacings are inline. Architect's recommendation is to map every visible style to existing UI-kit CSS Custom Properties; UX/UI-team should confirm there is no missing token before SCSS lands. | UX / UI lead | Gate 3 | open |
| 11 | Empty / loading / error / no-permission states — the design only depicts the "data present" state. The plugin must implement the inferred states (`SystemMessage` empty, preloader, `SystemMessage` error). Product/UX should review them before Gate 3 approval. | UX / PO | Gate 3 | open |

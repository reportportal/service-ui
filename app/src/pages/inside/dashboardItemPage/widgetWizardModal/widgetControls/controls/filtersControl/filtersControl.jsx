import classNames from 'classnames/bind';
import styles from './filtersControl.scss';

const cx = classNames.bind(styles);

export const FiltersControl = () => (
  <div className={cx('filters-control')}>Filter control will be soon...</div>
);

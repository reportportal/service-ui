info:
  build:
    name: UI Service
    description: ReportPortal User Interface
    version: '@version@'
    branch: '@branch@'

spring:
  profiles:
    active: ${rp.profiles:default}
  application:
    name: ui
